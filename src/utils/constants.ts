export const backend_url = "https://api.backpack.exchange/api/v1";
export const websocket_url = "wss://ws.backpack.exchange/";

export enum SocketManagerType {
  Ticker = "ticker",
  Depth = "depth",
}
